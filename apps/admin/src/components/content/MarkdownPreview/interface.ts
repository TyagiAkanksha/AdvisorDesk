// task-06 Interfaces: `{markdown: string}` in, visible text out — this contract is frozen.
// phase-3 task-04 swaps the `<pre>` placeholder for the shared markdown renderer without this
// props contract changing.
export interface MarkdownPreviewProps {
  markdown: string;
}
