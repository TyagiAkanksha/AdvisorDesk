import MuiTableHead from '@mui/material/TableHead';

import type { TableHeadProps } from './interface';

export default function Component(props: TableHeadProps) {
  return <MuiTableHead {...props} />;
}
