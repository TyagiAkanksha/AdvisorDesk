# vendored read-only reference code — never committed
reference_project/

# secrets & env (only .env.example is tracked)
.env
*.env
.env.*
!.env.example

# local tool state
.claude/settings.local.json
.superpowers/

# python
.venv/
__pycache__/

# node / next.js
node_modules/
.next/

# coverage
coverage/
.coverage
.coverage.*
htmlcov/

# phase-9 judge scorecard: the labelling-session worksheet is a generated scratch artifact,
# never committed (seed/judge_labels.yaml — the real, hand-labelled seed — is tracked as usual)
seed/judge_labels.pending.yaml
