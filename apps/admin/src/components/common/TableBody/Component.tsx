import MuiTableBody from '@mui/material/TableBody';

import type { TableBodyProps } from './interface';

export default function Component(props: TableBodyProps) {
  return <MuiTableBody {...props} />;
}
