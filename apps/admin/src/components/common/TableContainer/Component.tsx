import MuiTableContainer from '@mui/material/TableContainer';

import type { TableContainerProps } from './interface';

export default function Component(props: TableContainerProps) {
  return <MuiTableContainer {...props} />;
}
