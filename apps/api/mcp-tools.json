[
  {
    "description": "Metadata search over CMS content (title/tag/status \u2014 not vector search). Returns matching items and the total match count.",
    "inputSchema": {
      "additionalProperties": false,
      "description": "`search_content`'s arguments (PRD \u00a76): metadata search \u2014 title/tag/status, not vector.",
      "properties": {
        "limit": {
          "default": 10,
          "maximum": 100,
          "minimum": 1,
          "title": "Limit",
          "type": "integer"
        },
        "q": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Q"
        },
        "status": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Status"
        },
        "tag": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Tag"
        }
      },
      "title": "SearchContentArgs",
      "type": "object"
    },
    "name": "search_content"
  },
  {
    "description": "Count non-deleted content, optionally filtered by status and/or tag.",
    "inputSchema": {
      "additionalProperties": false,
      "description": "`count_content`'s arguments (PRD \u00a76): `status`/`tag`, both optional.",
      "properties": {
        "status": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Status"
        },
        "tag": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Tag"
        }
      },
      "title": "CountContentArgs",
      "type": "object"
    },
    "name": "count_content"
  },
  {
    "description": "Create a draft CMS content item. Creates missing tags (reactivating soft-deleted names). Always returns status 'draft' \u2014 never auto-published.",
    "inputSchema": {
      "additionalProperties": false,
      "description": "`create_draft`'s arguments (PRD \u00a76): `title`, `body_md`, `tags`.\n\n`title`'s validation mirrors `app.models.schemas.content.ContentCreate.title` exactly\n(`min_length=1` plus the same whitespace-only rejection) \u2014 the Goal's \"same behavior as\nthe REST routes by construction\" extends to input validation, not just the service calls.",
      "properties": {
        "body_md": {
          "default": "",
          "title": "Body Md",
          "type": "string"
        },
        "tags": {
          "default": [],
          "items": {
            "type": "string"
          },
          "title": "Tags",
          "type": "array"
        },
        "title": {
          "minLength": 1,
          "title": "Title",
          "type": "string"
        }
      },
      "required": [
        "title"
      ],
      "title": "CreateDraftArgs",
      "type": "object"
    },
    "name": "create_draft"
  },
  {
    "description": "Partially update a content item's title/body/tags. Re-embeds if the item is already published. The slug never changes.",
    "inputSchema": {
      "additionalProperties": false,
      "description": "`edit_content`'s arguments (PRD \u00a76): partial update \u2014 every field but `content_id`\noptional, mirroring `app.models.schemas.content.ContentUpdate`'s own `None`-means-\nunchanged contract and title validation.",
      "properties": {
        "body_md": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Body Md"
        },
        "content_id": {
          "format": "uuid",
          "title": "Content Id",
          "type": "string"
        },
        "tags": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Tags"
        },
        "title": {
          "anyOf": [
            {
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Title"
        }
      },
      "required": [
        "content_id"
      ],
      "title": "EditContentArgs",
      "type": "object"
    },
    "name": "edit_content"
  },
  {
    "description": "Soft-delete a content item (tombstone, not restorable) and remove its chunks in one transaction.",
    "inputSchema": {
      "additionalProperties": false,
      "description": "A single `content_id` argument (PRD \u00a76) \u2014 no other fields.",
      "properties": {
        "content_id": {
          "format": "uuid",
          "title": "Content Id",
          "type": "string"
        }
      },
      "required": [
        "content_id"
      ],
      "title": "ContentIdArgs",
      "type": "object"
    },
    "name": "delete_content"
  },
  {
    "description": "Add and/or remove tags on a content item in one call. Tags in `add` are created if missing (reactivating soft-deleted names).",
    "inputSchema": {
      "additionalProperties": false,
      "description": "`tag_content`'s arguments (PRD \u00a76): `content_id`, plus `add`/`remove` name lists.",
      "properties": {
        "add": {
          "default": [],
          "items": {
            "type": "string"
          },
          "title": "Add",
          "type": "array"
        },
        "content_id": {
          "format": "uuid",
          "title": "Content Id",
          "type": "string"
        },
        "remove": {
          "default": [],
          "items": {
            "type": "string"
          },
          "title": "Remove",
          "type": "array"
        }
      },
      "required": [
        "content_id"
      ],
      "title": "TagContentArgs",
      "type": "object"
    },
    "name": "tag_content"
  },
  {
    "description": "Publish a content item: runs the full publish transaction (\u00a74).",
    "inputSchema": {
      "additionalProperties": false,
      "description": "A single `content_id` argument (PRD \u00a76) \u2014 no other fields.",
      "properties": {
        "content_id": {
          "format": "uuid",
          "title": "Content Id",
          "type": "string"
        }
      },
      "required": [
        "content_id"
      ],
      "title": "ContentIdArgs",
      "type": "object"
    },
    "name": "publish"
  },
  {
    "description": "Archive a content item: status -> archived, removes its chunks.",
    "inputSchema": {
      "additionalProperties": false,
      "description": "A single `content_id` argument (PRD \u00a76) \u2014 no other fields.",
      "properties": {
        "content_id": {
          "format": "uuid",
          "title": "Content Id",
          "type": "string"
        }
      },
      "required": [
        "content_id"
      ],
      "title": "ContentIdArgs",
      "type": "object"
    },
    "name": "archive"
  },
  {
    "description": "Report client questions the content didn't cover: user messages whose next assistant reply found no matching content (retrieval_found=false), asked within the last `days` days, newest first.",
    "inputSchema": {
      "additionalProperties": false,
      "description": "`report_content_gaps`'s arguments (PRD \u00a76): `days`/`limit`, both optional.",
      "properties": {
        "days": {
          "default": 30,
          "minimum": 1,
          "title": "Days",
          "type": "integer"
        },
        "limit": {
          "default": 20,
          "maximum": 100,
          "minimum": 1,
          "title": "Limit",
          "type": "integer"
        }
      },
      "title": "ReportContentGapsArgs",
      "type": "object"
    },
    "name": "report_content_gaps"
  },
  {
    "description": "Report client questions the system answered BADLY, grouped and classified: negative_feedback (a client pressed thumbs-down), refused (nothing in the corpus was close), near_miss (a source was just under the retrieval threshold, or retrieval succeeded and the answer still declined \u2014 either way the corpus nearly had it), low_confidence (answered, but only just above the threshold). Newest `days` days, most-asked first. Use this to decide what content to write or expand next.",
    "inputSchema": {
      "additionalProperties": false,
      "description": "`report_weak_queries`'s arguments (phase-9 DESIGN \u00a7D): `days`/`limit`, both optional.\n\nDeliberately NO `threshold` argument: the band boundaries are only meaningful against the\nthreshold the answers were actually SERVED under, which is deployment config\n(`Settings.similarity_threshold`), not a caller's choice. Exposing it would let the agent\n(or a curious connector) re-score history against a threshold that never ran.",
      "properties": {
        "days": {
          "default": 30,
          "minimum": 1,
          "title": "Days",
          "type": "integer"
        },
        "limit": {
          "default": 20,
          "maximum": 100,
          "minimum": 1,
          "title": "Limit",
          "type": "integer"
        }
      },
      "title": "ReportWeakQueriesArgs",
      "type": "object"
    },
    "name": "report_weak_queries"
  },
  {
    "description": "Record a proposed content fix for detected weak queries and create its draft. Pass either kind (new_article/expand_article/retune) or failure_cause (from the eval failure taxonomy) and the weak-query rows as evidence. Creates a DRAFT only \u2014 publishing is a separate, explicitly requested step.",
    "inputSchema": {
      "additionalProperties": false,
      "description": "`propose_content_fix`'s arguments (phase-9 DESIGN \u00a7D).",
      "properties": {
        "evidence": {
          "default": [],
          "items": {
            "additionalProperties": true,
            "type": "object"
          },
          "title": "Evidence",
          "type": "array"
        },
        "failure_cause": {
          "anyOf": [
            {
              "enum": [
                "retrieval_miss",
                "corpus_gap",
                "threshold_refusal",
                "generation_unfaithful",
                "judge_disagreement"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Failure Cause"
        },
        "kind": {
          "anyOf": [
            {
              "enum": [
                "new_article",
                "expand_article",
                "retune"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Kind"
        },
        "rationale": {
          "minLength": 1,
          "title": "Rationale",
          "type": "string"
        },
        "target_content_id": {
          "anyOf": [
            {
              "format": "uuid",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Target Content Id"
        },
        "title": {
          "minLength": 1,
          "title": "Title",
          "type": "string"
        }
      },
      "required": [
        "title",
        "rationale"
      ],
      "title": "ProposeContentFixArgs",
      "type": "object"
    },
    "name": "propose_content_fix"
  },
  {
    "description": "List content-fix proposals, newest first, optionally filtered by status (proposed/accepted/rejected).",
    "inputSchema": {
      "additionalProperties": false,
      "description": "`list_proposals`'s arguments. `limit` defaults to 20, not the service's own 50: a tool\nresult lands in a model's context window, so the MCP default mirrors\n`tools_gaps._DEFAULT_LIMIT`/`_MAX_LIMIT` (20/100) rather than the service default. Deliberate,\nnot a drift.",
      "properties": {
        "limit": {
          "default": 20,
          "maximum": 100,
          "minimum": 1,
          "title": "Limit",
          "type": "integer"
        },
        "status": {
          "anyOf": [
            {
              "enum": [
                "proposed",
                "accepted",
                "rejected"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Status"
        }
      },
      "title": "ListProposalsArgs",
      "type": "object"
    },
    "name": "list_proposals"
  },
  {
    "description": "Accept a proposal \u2014 allowed ONLY when the eval data supports it: a baseline run and the eval_run_after_id run must both exist, the after-run must have measured a changed corpus, pct_fully_supported must not have dropped, and the run must show no regressions. Otherwise this fails with a conflict explaining which check said no.",
    "inputSchema": {
      "additionalProperties": false,
      "description": "`accept_proposal`'s arguments (phase-9 DESIGN \u00a7D).",
      "properties": {
        "eval_run_after_id": {
          "format": "uuid",
          "title": "Eval Run After Id",
          "type": "string"
        },
        "proposal_id": {
          "format": "uuid",
          "title": "Proposal Id",
          "type": "string"
        }
      },
      "required": [
        "proposal_id",
        "eval_run_after_id"
      ],
      "title": "AcceptProposalArgs",
      "type": "object"
    },
    "name": "accept_proposal"
  },
  {
    "description": "Reject a proposal with a reason; if its draft was published, it is archived so the change leaves the corpus.",
    "inputSchema": {
      "additionalProperties": false,
      "description": "`reject_proposal`'s arguments (phase-9 DESIGN \u00a7D).",
      "properties": {
        "proposal_id": {
          "format": "uuid",
          "title": "Proposal Id",
          "type": "string"
        },
        "reason": {
          "minLength": 1,
          "title": "Reason",
          "type": "string"
        }
      },
      "required": [
        "proposal_id",
        "reason"
      ],
      "title": "RejectProposalArgs",
      "type": "object"
    },
    "name": "reject_proposal"
  }
]
